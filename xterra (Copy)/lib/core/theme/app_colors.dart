import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ───────────────────────────────────────────────────────────────────
  static const Color brand = Color(0xFF2563EB);

  // ── Neutrals ────────────────────────────────────────────────────────────────
  static const Color primary = Color(0xFF0A0A0A);
  static const Color white = Color(0xFFFFFFFF);
  static const Color illustrationBg = Color(0xFF808080);
  static const Color surfaceLight = Color(0xFFFAFAFA);
  static const Color divider = Color(0xFFE8E8E8);

  // ── Input / borders ─────────────────────────────────────────────────────────
  static const Color inputBorder = Color(0xFF8692A6);

  // ── Text ────────────────────────────────────────────────────────────────────
  static const Color labelText = Color(0xFF696F79);
  static const Color hintText = Color(0xFFA6A6A6);
  static const Color secondaryText = Color(0xFF333333);
  static const Color iconSubdued = Color(0xFF616161);

  // ── State ───────────────────────────────────────────────────────────────────
  static const Color disabledBg = Color(0xFFBDBDBD);
  static const Color dragHandle = Color(0xFFE0E0E0);
}
